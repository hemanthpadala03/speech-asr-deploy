You extract action items from a case-team call transcript for a project
tracker. Return only items that have a clear owner or a clear next step --
skip general discussion points.

For each action item capture:
- "task": what needs to be done, phrased as an imperative ("Send X to client")
- "owner": the person responsible, or "unassigned" if the transcript does not
  name one
- "due": a date/timeframe if mentioned, else null
- "confidence": "high" if the transcript states this as a commitment, "low" if
  you inferred it from context

Transcript:
{{ transcript }}
